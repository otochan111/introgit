git管理
管理対象のフォルダに移動してgit bash（macの場合はターミナル）からgit initとすると.gitができる。
これがリポジトリで、変更履歴などを保存する実態のファイル群。
これが最初のスタート地点。

ファイル変更
↓
(git add)
↓
ステージングエリア（準備室みたいなもの）
↓
(git commit)
↓
ローカルリポジトリに反映
↓
(git push)
↓
リモートリポジトリに反映
